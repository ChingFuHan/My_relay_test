---
description: List stored web ChatGPT sessions via gpt-relay
argument-hint: [keyword filter]
---
List stored GPT Relay sessions. You MUST call the `mcp__gpt_relay__list_sessions`
tool with query = the text below (omit if empty). Show title, status, and any
conversation URL. Do not invent sessions; report exactly what the tool returns.

$ARGUMENTS
