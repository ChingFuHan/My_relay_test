---
description: List stored web Gemini sessions via gemini-relay
argument-hint: [keyword filter]
---
List stored Gemini Relay sessions. You MUST call the
`mcp__gemini_relay__list_sessions` tool with query = the text below (omit if
empty). Show title, status, and any conversation URL. Do not invent sessions;
report exactly what the tool returns.

$ARGUMENTS
