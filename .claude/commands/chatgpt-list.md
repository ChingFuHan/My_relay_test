---
description: List stored gpt-relay ChatGPT sessions
argument-hint: [keyword filter]
allowed-tools: mcp__gpt-relay__list_sessions
---
List stored gpt-relay sessions. Call `mcp__gpt-relay__list_sessions` with query =
the text below (omit if empty). Show title, status, and any conversation URL.

$ARGUMENTS
