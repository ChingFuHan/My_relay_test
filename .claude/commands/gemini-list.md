---
description: List stored gemini-relay Gemini sessions
argument-hint: [keyword filter]
allowed-tools: mcp__gemini-relay__list_sessions
---
List stored gemini-relay sessions. Call `mcp__gemini-relay__list_sessions` with
query = the text below (omit if empty). Show title, status, and any conversation URL.

$ARGUMENTS
